import styled from 'styled-components'

export const BackToTop = styled.img`
	position: fixed;
	bottom: 2.4rem;
	right: 2.4rem;
	width: 4.8rem;
	height: 4.8rem;
	object-fit: contain;
	cursor: pointer;
`
